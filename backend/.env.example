# Conexão usada pela aplicação (em produção: pooler do Supabase, porta 6543)
DATABASE_URL="postgresql://admin:admin123@localhost:5432/estacionamento?schema=public"
# Conexão direta usada por `prisma migrate` (local: igual à DATABASE_URL)
DIRECT_URL="postgresql://admin:admin123@localhost:5432/estacionamento?schema=public"
PORT=3001
# Origens extras permitidas no CORS (separadas por vírgula)
FRONTEND_URL=http://localhost:3000
